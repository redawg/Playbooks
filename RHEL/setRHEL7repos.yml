---
- name: Set Default Repos for RHEL 7
  hosts: all
  gather_facts: False
  vars:

  tasks:
    - name: Disable all repo
      yum_repository:
        name: *
        state: absent
      notify: yum-clean-metadata
      

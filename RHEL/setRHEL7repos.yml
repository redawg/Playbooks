---
- name: Set Default Repos for RHEL 7
  hosts: all
  gather_facts: False
  vars:

  tasks:
    - name: Disable rhel-7-server-htb-rpms repo
      yum_repository:
        name: rhel-7-server-htb-rpms/x86_64
        state: absent
      notify: yum-clean-metadata

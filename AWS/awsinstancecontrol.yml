---
  - name: Control AWS instances
    hosts: localhost
    connection: local
    gather_facts: no
    vars:
       region: ''
       state: ''
      


    tasks:
    - name: Setting ec2 instances with {{filter}} to {{state}}
      ec2:
        region: "{{region}}"
        state: "{{state}}"
        instance_tags:
            Name: "{{servername}}"

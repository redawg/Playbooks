---
  - name: Adding Subnet to Network {{az_rgname}}
      azure_rm_subnet:
        resource_group: "{{az_rgname}}"
        name: "{{az_rgname}}{{'_net1'}}"
        address_prefixe: "10.10.10.0/25"
        virtual_network: "{{az_rgname}}"
        tags:
            created: ansbile
            delete: never
            demo: "{{customer}}"

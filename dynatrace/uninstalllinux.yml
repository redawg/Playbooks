---
- name: Uninstall OneAgent installer in specific version to a custom
  gather_facts: False
  hosts: all


  tasks:
    - name: Uinstall Dynatrace OneAgent
      ansible.builtin.command:
        cmd: "/opt/dynatrace/oneagent/agent/uninstall.sh"
